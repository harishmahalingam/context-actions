name: ENV-CONTEXT
on: push
env:
  NAME: ABHSHEK
  env_var: ${{ vars.ACTIONS }}
  
jobs:
  linux_job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Hi ${{ env.NAME }}'  
          echo 'Session-number': $env_var
          echo '${{job.status}}'
    env:
      NAME: harish
